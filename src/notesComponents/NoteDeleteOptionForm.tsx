import React from "react";

const NoteDeleteOptionForm = ({
  permanentDelete,
  cancelDelete,
  noteId,
  noteTitle,
}: any) => {
  return <div>NoteDeleteOptionForm</div>;
};

export default NoteDeleteOptionForm;
